name: build

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run build script
        run: ./script/build
