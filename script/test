#!/bin/bash

set -euo pipefail

echo "Testing..."
contents="$(cat build/docs/main.txt)"

if [[ "$contents" != "Hello, World!!!" ]]; then
  echo "Unexpected build/docs/main.txt contents: $contents" >&2
  exit 1
fi
